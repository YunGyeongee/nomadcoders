/* 배열 */
const week = ['mon', 'tue', 'wed', 'thu', 'fri', 'sat'];
const nonsense = [1, 2, "hello", false, null, true, undefined, "Rachel"]

// Get Item from Array
console.log(week);

// Add one more day to the array
week.push('sun');
console.log(week);